# Docker ITB Deployment Configuration
# Modify these values for your environment

# Docker container information
CONTAINER_NAME=srv                    # ITB service container (handles validation)
DOMAIN_NAME=fhir-validation           # Domain name for the plugin
DOMAINS_PATH=/opt/itb/domains        # Path to domains inside srv container

# Alternative common paths (uncomment the one that matches your setup):
# DOMAINS_PATH=/var/lib/itb/domains
# DOMAINS_PATH=/home/itb/domains
# DOMAINS_PATH=/usr/local/itb/domains

# Plugin information (usually don't need to change these)
PLUGIN_JAR=target/json-fhir-validator-0.1.0-SNAPSHOT.jar
PLUGIN_NAME=json-fhir-validator-0.1.0-SNAPSHOT.jar

# Note: ITB consists of 4 containers:
# - srv: Service/backend container (where plugins are deployed)
# - ui: User interface container
# - mysql: Database container
# - redis: Cache container
